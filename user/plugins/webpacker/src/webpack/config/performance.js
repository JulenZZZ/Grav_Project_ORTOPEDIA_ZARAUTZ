// PERFORMANCE CONFIG
// ––––––––––––––––––––––

module.exports = () => {
  const performance = {
    // Turns OFF error when hints are found (an asset is over 250kb)
    hints: false
  }

  return performance
}
